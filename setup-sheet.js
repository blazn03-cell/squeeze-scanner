console.log("Google Sheets setup has been removed from APEX Scanner v2.");
console.log("Persist scan_runs and scan_results in PostgreSQL, then export reports to object storage when needed.");
console.log("Run `npm run scan` to generate a mocked local snapshot.");
