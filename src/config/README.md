# Runtime configuration

Configuration should read server-only environment variables, validate required values at startup, and default to `MARKET_DATA_PROVIDER=mock` until provider contracts are confirmed.
